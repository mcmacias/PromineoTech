// Stars: a one to five-star rating component that allows users to rate something (movies in 
// this case, but remember that components are reusable, so you could use it elsewhere!)

import React from "react";

export default class Stars extends React.Component{
  render() {
    return "Stars"
  };
}